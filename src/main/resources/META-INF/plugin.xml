<idea-plugin>
    <id>com.pino.intellij-peek-definition</id>
    <name>Peek Definition</name>
    <vendor>Pino</vendor>
    <description><![CDATA[
    <p><b>Peek Definition</b> for Java. It shows the definition of the symbol at the
    caret right below the current line, inside the editor you are working in, so you can read an
    implementation without leaving your file or losing your place.</p>

    <p>Unlike Quick Definition (<code>Ctrl+Shift+I</code>), the Peek is not a popup: it does not cover your
    code, it stays open while you keep working, and you can open several at once.</p>

    <h3>Features</h3>
    <ul>
      <li><b>Embedded in the editor</b>: the definition opens between the lines of your code, with the
      lines below moved down instead of hidden.</li>
      <li><b>Real editor, real file</b>: the Peek shows the whole definition file, scrolled to the
      definition, which is highlighted. It uses your color scheme, font and theme, and updates live
      when the file changes.</li>
      <li><b>Accurate</b>: definitions are resolved the same way as <i>Go to Declaration</i>, so overloads,
      constructors and library classes (attached or decompiled sources) resolve correctly.</li>
      <li><b>Several Peeks at once</b>: peek from different lines and every Peek stays open. Peeking
      from inside a Peek replaces its content with the new definition.</li>
      <li><b>Read-only</b>: opening, switching and closing a Peek never modifies your code.</li>
    </ul>

    <h3>Usage</h3>
    <ul>
      <li>Put the caret on a method call or constructor and choose <b>Peek Definition</b> from the editor's
      right-click menu or from <b>Navigate</b>.</li>
      <li>Click the Peek's title bar to collapse or expand it. Double-click it to open the file in a
      regular editor tab.</li>
      <li><b>Esc</b> inside a Peek (or its <b>&times;</b> button) closes that Peek. <b>Esc</b> in the
      editor closes all of its Peeks.</li>
    </ul>

    <p>No shortcut is assigned by default. Bind one in <b>Settings | Keymap | Peek Definition</b>.</p>
    ]]></description>

    <change-notes><![CDATA[
    <p><b>0.0.1</b></p>
    <ul>
      <li>Initial release: Peek Definition for Java, embedded below the current line.</li>
    </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <editorFactoryListener implementation="com.pino.peekdefinition.lifecycle.PeekEditorFactoryListener"/>
        <editorActionHandler action="EditorEscape" order="first"
                             implementationClass="com.pino.peekdefinition.action.PeekEscapeHandler"/>
    </extensions>

    <actions>
        <!-- No default shortcut: users bind one in Settings | Keymap. -->
        <action id="com.pino.peekdefinition.PeekDefinition"
                class="com.pino.peekdefinition.action.PeekDefinitionAction"
                text="Peek Definition"
                description="Show the definition of the symbol at the caret inside the current editor">
            <add-to-group group-id="EditorPopupMenu1" anchor="before" relative-to-action="EditorPopupMenu.GoTo"/>
            <add-to-group group-id="GoToCodeGroup" anchor="after" relative-to-action="GotoDeclaration"/>
        </action>
    </actions>
</idea-plugin>
